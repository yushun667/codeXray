# 占位功能记录

凡当前仅作为功能占位、尚未完成实现的函数或代码段，在此统一登记。

## 登记格式

- **模块**：所属仓库/文件
- **位置**：函数名或代码段标识
- **说明**：占位原因与后续实现要点
- **状态**：待实现 / 进行中 / 已完成

---

## 当前登记项

### 1. codexray.openProject

- **模块**：主仓库 / src/extension.ts
- **位置**：命令 `codexray.openProject` 注册实现
- **说明**：当前仅执行 `vscode.openFolder` 打开文件夹选择，未与解析引擎「工程根」持久化绑定；后续可将选定路径写入配置或 workspaceState，供解析引擎 --project 使用。
- **状态**：待实现

### 2. codexray.focusVisualization

- **模块**：主仓库 / src/extension.ts
- **位置**：命令 `codexray.focusVisualization` 注册实现
- **说明**：当前仅弹出提示「请从编辑器右键或查询命令打开可视化标签后，点击对应标签聚焦」；后续可维护最近打开的可视化 WebviewPanel 引用并调用 `panel.reveal()` 实现聚焦。
- **状态**：待实现

### 3. 图的折叠/展开（useGraphCollapse）

- **模块**：resources/ui / src/visualization
- **位置**：计划中的 useGraphCollapse hook 与 GraphCore 集成
- **说明**：按 doc/02 规划，图支持节点折叠/展开；当前仅实现右键「继续查询前置/后置」与 graphAppend 合并，折叠展开留待后续迭代。
- **状态**：待实现

### 4. 解析引擎 AST 真实解析（Clang）

- **模块**：parser / src/ast
- **位置**：ast/call_graph、function_pointer、class_relation、data_flow、control_flow
- **说明**：五类 AST 均在 CODEXRAY_HAVE_CLANG 下实现并接入 driver 与 DB。call_graph：FrontendAction + RecursiveASTVisitor，symbol 与 call_edge（direct/via_function_pointer）。function_pointer：按调用点函数指针类型在 TU 内保守枚举可能 callee。class_relation：CXXRecordDecl 遍历，class 与 inheritance。data_flow：文件作用域全局变量与读写点（DeclRefExpr 为读，BinaryOperator/UnaryOperator 赋值为写），DataFlowEdgeRecord 使用 var_usr/reader_usr/writer_usr。control_flow：CFG::buildCFG  per 函数，输出基本块节点与 control 边。DBWriter 已提供 WriteClasses、WriteClassRelations、WriteGlobalVars、WriteDataFlowEdges、WriteCfgNodes、WriteCfgEdges；driver 汇总并写入全部六类数据。
- **状态**：已完成
